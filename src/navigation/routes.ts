const routes = Object.freeze({
    auth: {
        login: 'login',
    },
    app: {
        // tabs: 'tabs',
        // drawer: 'drawer',

        // dashboard: {
            dashboard: 'dashboard',
        // },

    },
});

export default routes;
